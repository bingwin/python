# -*- coding: utf-8 -*-
# Python中默认的编码格式是 ASCII 格式，在没修改编码格式时无法正确打印汉字，所以在读取中文时会报错。

l1 = [1, 1, 2, 3, 4, 5, 4]
x = set([1, 2, 3, 4, 1, 1])
print(x)

# 请写出一段Python代码实现删除一个list里面的重复元素？

l1 = [1, 1, 2, 3, 4, 5, 4]
s = list(set(l1))
print(s)

